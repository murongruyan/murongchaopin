# v2.9.28 更新内容

1. 修复一加 Ace 6（PLQ110）"完美禁用 ADFR"按钮无法生效的问题：该机型面板驱动自报 ADFR 不支持、内核锁模块注定无法加载，现在改为 props 禁用方案（与一加 12 相同路径，重启后生效），按钮点击后正确保存并显示生效状态。
2. 修复 Ace 6 上策略保存并重启后，WebUI 按钮仍显示未选中的问题（策略回读缺少该机型的映射分支）。
3. 修复开机阶段在 Ace 6 上残留内核锁模块加载失败日志的问题（开机链按机型分流，Ace 6 不再触碰注定失败的内核模块，直接走 props 方案）。
4. 内置 daemon 版本同步至 2.9.28。
