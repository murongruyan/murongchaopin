# v2.9.29 更新内容

1. 修复一加 Ace 6（PLQ110）"完美禁用 ADFR"在保存并重启后一直显示"待重启"的问题：props 禁用方案实际已生效，但状态回读把 props 运行标记的路径指向了模块根目录而不是付费包目录，导致生效状态永远检测不到。现在路径已统一，按钮会正确显示为已生效。
2. 日志收集脚本同步修正 props 状态目录路径，后续日志包能看到 props 方案的真实运行状态。
3. 内置 daemon 版本同步至 2.9.29。
