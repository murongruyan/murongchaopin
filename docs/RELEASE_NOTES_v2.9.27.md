# v2.9.27 更新内容

1. 修复"禁用日常 LTPO"开关在开机后首次打开时，自制 LTPO 内核模块不在运行时加载、息屏 1Hz 降档在重启前完全不生效的问题；现在打开开关或切换子方案会立即在运行时补加载模块（含触摸升频探针，已加载时自动跳过）。
2. 修复息屏降档停在其他低档、无法到达 1Hz 的问题（KO 注入低档后从 165Hz 到 1Hz 的完整阶梯超出原步数上限）。
3. 修复运行时补加载自制模块后，daemon 需要重启才能识别新注入的 1/10/30Hz 档位的问题；刷新率重放时现在会同步重新枚举模式表。
4. 内置 daemon 版本同步至 2.9.27。
