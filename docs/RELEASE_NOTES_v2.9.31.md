# v2.9.31 更新内容

1. 修复一加 Ace 6（PLQ110）"完美禁用 ADFR"实际已生效但按钮仍显示"待重启"的问题：props 禁用（persist 属性）跨重启持续有效，生效判定不再要求本开机重新写入标记，只要最后一次应用成功即显示已生效。
2. WebUI 日志页新增显示策略判定链现场证据（策略读回实际输出、安装脚本指纹、语法与换行符检查），此类显示问题一个日志包定论。
3. 修复日志脚本在 toybox 环境下的两个采集缺陷（版本信息空缺、部分段落缺失）。
4. 内置 daemon 版本同步至 2.9.31。
