# 慕容显示增强 v2.9.24

本版本相对 `v2.9.23` 的有效更新：

- 修复已安装付费资源包缺少旧版 `rmx5200_ltpo_modes.sha256` 旁路文件时，WebUI 错误拒绝重新启用自制 LTPO 的问题。
- 自制 LTPO 继续使用付费包安装阶段完成的整包 SHA-256、Manifest 文件清单和逐文件校验；运行时不再要求未纳入付费包契约的重复 hash 文件。
- 保留 DRM-KO、ADFR 重启加载、实时刷新率设置和 DPI 不变路径。
