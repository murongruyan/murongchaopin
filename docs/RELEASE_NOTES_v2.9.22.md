# 慕容显示增强 v2.9.22

本版对比已发布的 v2.9.21，更新免费包内置产物、刷新率配置逻辑和显示设置稳定性。

- `mode.txt` 作为全局/应用刷新率唯一真值；设置页、自定义应用刷新率、搜索和游戏助手共享同一份配置回显，刷新率路径不修改 DPI 或分辨率密度。
- 系统应用列表切换改为后台加载、主线程提交并强制刷新列表，搜索结果与主列表共用同一份结果。
- RMX5200 DRM 高刷路径继续使用已验证的逐档链路时钟与源 timing，不启用固定最大链路时钟实验。
- 免费包内置 Hook、daemon 和 DRM-KO 产物同步更新，并通过构建、签名和兼容性校验。
