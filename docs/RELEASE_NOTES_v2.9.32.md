# v2.9.32 更新内容

1. 修复一加 Ace 6（PLQ110）KO 无法加载的问题：按 Ace6 真机内核 ABI 重新生成符号 CRC（module_layout 等 13 处与公开源码构建不同的符号全部修正），KO 锁与 DRM 扩展档位在 Ace6 上恢复可用；完美禁用 ADFR 恢复 KO 锁路径（props 方案保留为兜底）。
2. WebUI 显示参数按机型驱动：超频档判定、风险警告文案、分辨率档位标签不再硬编码 GT8 Pro 实测值；一加 15（165Hz 原生）等机型不再误标超频档。
3. 修复日志脚本 toybox 环境的两个采集缺陷；新增 kcore/KO 探针取证段。
4. 内置 daemon 版本同步至 2.9.32。
