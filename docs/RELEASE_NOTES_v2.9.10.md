# 慕容显示增强 v2.9.10

本版本调整付费组件下载和安装契约：下载仍然只能通过慕容调度授权接口取得一次性令牌，安装前继续验证整包 SHA-256、Manifest 文件列表、目标路径、文件大小、权限和逐文件 SHA-256。旧版带 Manifest 签名的付费包继续兼容；新付费包不再要求额外的生产签名私钥。

同时更新基础模块安装器，使其能够安装新的无签名付费包，并继续使用原子替换和完整重启流程。
